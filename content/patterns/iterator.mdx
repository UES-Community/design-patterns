---
title: Iterator
category: comportamiento
difficulty: principiante
description: Proporciona una forma de acceder secuencialmente a los elementos de un objeto agregado sin exponer su representación subyacente.
also_known_as: ["Cursor"]
tags: ["recorrido", "colección", "secuencia"]
languages: ["typescript", "python", "go", "java"]
---

## Propósito

El **Iterator** abstrae la forma en que se recorre una colección. El cliente no necesita saber si la colección es un array, un árbol, un grafo o una base de datos — solo llama a `next()`.

## Cuándo usar

- Cuando quieres ocultar la implementación interna de una colección.
- Cuando quieres soportar múltiples formas de recorrer la misma colección.
- Cuando quieres proporcionar una interfaz uniforme para recorrer diferentes tipos de colecciones.

```typescript
// TypeScript — Iterator para árbol binario (in-order traversal)
class TreeNode<T> {
  constructor(
    public value: T,
    public left?: TreeNode<T>,
    public right?: TreeNode<T>,
  ) {}
}

class InOrderIterator<T> implements Iterator<T> {
  private stack: TreeNode<T>[] = []
  private current: TreeNode<T> | undefined

  constructor(root?: TreeNode<T>) {
    this.current = root
  }

  next(): IteratorResult<T> {
    while (this.current || this.stack.length > 0) {
      while (this.current) {
        this.stack.push(this.current)
        this.current = this.current.left
      }
      const node = this.stack.pop()!
      this.current = node.right
      return { value: node.value, done: false }
    }
    return { value: undefined as T, done: true }
  }

  [Symbol.iterator](): Iterator<T> {
    return this
  }
}

class BinarySearchTree<T extends number> {
  private root?: TreeNode<T>

  insert(value: T): void {
    const insert = (node: TreeNode<T> | undefined): TreeNode<T> => {
      if (!node) return new TreeNode(value)
      if (value < node.value) node.left = insert(node.left)
      else if (value > node.value) node.right = insert(node.right)
      return node
    }
    this.root = insert(this.root)
  }

  [Symbol.iterator](): Iterator<T> {
    return new InOrderIterator(this.root)
  }
}

// Uso
const bst = new BinarySearchTree<number>()
;[5, 3, 7, 1, 4, 6, 8].forEach(n => bst.insert(n))

for (const value of bst) {
  process.stdout.write(`${value} `) // 1 3 4 5 6 7 8
}
```

```python
# Python — Iterador paginado para API REST
from typing import TypeVar, Generic, Iterator, Optional
from dataclasses import dataclass

T = TypeVar('T')

@dataclass
class Page(Generic[T]):
    items: list[T]
    next_cursor: Optional[str]
    total: int

class PaginatedIterator(Generic[T]):
    def __init__(self, fetch_page, page_size: int = 10):
        self._fetch = fetch_page
        self._page_size = page_size
        self._cursor: Optional[str] = None
        self._items: list[T] = []
        self._index = 0
        self._exhausted = False

    def __iter__(self) -> Iterator[T]:
        return self

    def __next__(self) -> T:
        if self._index >= len(self._items):
            if self._exhausted:
                raise StopIteration

            page = self._fetch(cursor=self._cursor, size=self._page_size)
            self._items = page.items
            self._cursor = page.next_cursor
            self._exhausted = page.next_cursor is None
            self._index = 0

            if not self._items:
                raise StopIteration

        item = self._items[self._index]
        self._index += 1
        return item

# Uso
def fetch_users(cursor=None, size=10):
    users = [{'id': i, 'name': f'User {i}'} for i in range(1, 26)]
    start = int(cursor or 0)
    page_items = users[start:start + size]
    next_c = str(start + size) if start + size < len(users) else None
    return Page(page_items, next_c, len(users))

for user in PaginatedIterator(fetch_users, page_size=10):
    print(user['name'])
```

```go
// Go — Iterador de archivos con filtro
package iterator

import (
    "io/fs"
    "path/filepath"
)

type FileIterator struct {
    paths   []string
    index   int
    filter  func(string) bool
}

func NewFileIterator(root string, filter func(string) bool) (*FileIterator, error) {
    var paths []string
    err := filepath.WalkDir(root, func(path string, d fs.DirEntry, err error) error {
        if err != nil || d.IsDir() {
            return err
        }
        if filter == nil || filter(path) {
            paths = append(paths, path)
        }
        return nil
    })
    return &FileIterator{paths: paths, filter: filter}, err
}

func (f *FileIterator) HasNext() bool    { return f.index < len(f.paths) }
func (f *FileIterator) Next() string     { path := f.paths[f.index]; f.index++; return path }
func (f *FileIterator) Reset()           { f.index = 0 }
```

```java
// Java — Iterador para colección personalizada
import java.util.Iterator;
import java.util.NoSuchElementException;

public class Range implements Iterable<Integer> {
    private final int start;
    private final int end;
    private final int step;

    public Range(int start, int end, int step) {
        this.start = start;
        this.end = end;
        this.step = step;
    }

    @Override
    public Iterator<Integer> iterator() {
        return new Iterator<>() {
            private int current = start;

            @Override
            public boolean hasNext() {
                return step > 0 ? current < end : current > end;
            }

            @Override
            public Integer next() {
                if (!hasNext()) throw new NoSuchElementException();
                int value = current;
                current += step;
                return value;
            }
        };
    }
}

// Uso
for (int i : new Range(0, 10, 2)) {
    System.out.print(i + " "); // 0 2 4 6 8
}
```
